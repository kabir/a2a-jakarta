a2a.authorization.required=false
# Skip transport validation — this JSON-RPC deployment does not include the HTTP_JSON transport the shared card advertises
org.a2aproject.sdk.transport.skipValidation=true
